# dc-loot-editor

## 1.30.0

### Minor Changes

- 3a4416a: 阿卡伊農加上「上個門檻已過多久」：跨過 80/60/40/20 後開始計時，用來反推隊友技能的冷卻好了沒。換王或重置會清掉。
- 3a4416a: 輸出速度改得更準，並加上峰值：回血不再倒扣（只累計掉血的部分，王邊回血邊打時不會被低估）；新增 60 秒內與本條血的峰值速度，機制打斷、跑位、王無敵讓當下速度歸零時，仍看得出打得順時有多快。整條計算都綁在血條左邊那段（剩餘血量）的顏色上——換階段或換王時血條會換色，序列、當下速度、峰值樣本一律不跨色計算，右邊那段已打掉露出的底色不參與。

## 1.29.1

### Patch Changes

- 2106be2: 子母畫面裡的內容一律不能反白選取，避免連點按鈕時整片文字被選起來；輸入遊戲計時的欄位不受影響，照樣能編輯與選取。

## 1.29.0

### Minor Changes

- 4dc89e9: 機制計算機新增「阿卡伊農」與第三種機制模板「血量」：不看時間只看血量，掉到 80/60/40/20 就會出招。面板主角是距離下一個門檻還有幾 %，進到提前量（預設 5%，可調）轉成警戒色，跨過門檻時響鈴並標記已過；一口氣掉很多時中間跨過的門檻也會全部標到。血量讀數與「王血量」擷取共用，所以要先開擷取。

### Patch Changes

- 4dc89e9: 王血量的每秒傷害量改成只要有讀數就顯示（沒在掉血時顯示 0.0%），原本沒掉血就整個隱藏，看起來像功能不見了。

## 1.28.4

### Patch Changes

- 7b6d7e4: 修正血量固定讀成 100% 的問題：血條下方的深色 UI 橫帶跟空槽一樣是低飽和的中灰、又一樣長，被當成血條的一部分，範圍一垮，右端的整欄檢查就在空槽處失敗、把空槽整段排除。改成挑血條時有血色優先、中線取有血色最多的那一列、上下界用色相收住。實測兩張同一情境的實戰截圖都從 100% 修正為 96.9%。
- 7b6d7e4: 修正三件子母畫面的問題：一是選到女皇時 `now` 停止更新，導致遊戲計時對齊後顯示的時間會愈按愈偏（輸入 44:00 卻顯示 44:23）；二是把遊戲計時放進原本「重置」那一列（左對齊、右重置），不再多出一顆重置按鈕；三是女皇的「接下來」時間表拆成獨立元件留在主視窗——子母畫面是把面板搬過去而不是複製，原本整個面板連同時間表一起被搬走，主視窗就空了。

## 1.28.3

### Patch Changes

- 0ff229d: 修正畫面被縮小時血量讀成 100%（甚至完全讀不到）的問題：縮圖後血條左端外框旁會糊出一片跟空槽同色的陰影，偵測到的起點落在那裡，往右第二格就撞上左外框而提前結束。改成先往右找出血條內容真正的起點再開始判讀，另外框到的範圍不足畫面寬 10% 時直接視為沒找到。實測同一張實戰截圖，原尺寸與縮到 1280 的讀值一致（96.9% / 97.0%）。

## 1.28.2

### Patch Changes

- f3f6630: 子母畫面補上遊戲計時對齊（輸入、對齊、±1 秒校準與跟著跑的時鐘），並把版面壓緊：間距全面收窄、小視窗裡的贅字收掉、時鐘列與血量卡並排。女皇五格全開從 321px 降到 236px（縮放 0.62 → 0.85），反盾王 154px 不需縮放。另外修正女皇按下觸發後卡片突然撐高、整排高低不齊的問題——未觸發的卡片現在也保留同一組欄位。

## 1.28.1

### Patch Changes

- 506675e: 子母畫面改成 480×200，內容等比縮到剛好塞滿視窗、不出捲軸（換王、開始擷取或拉動視窗都會重算）。女皇在小視窗改排成一列，避免疊成多排把整體撐高、縮下來字小到看不清。

## 1.28.0

### Minor Changes

- 0624869: 機制計算機新增「子母畫面」：把王血量與當前王的機制面板搬到一個置頂的小視窗，打王時遊戲在前景也看得到倒數與血量。反盾王連操作按鈕一起帶過去，女皇則是五個機制的觸發鈕（小視窗自動排成兩欄）；事件表與參數留在頁面。關掉小視窗內容會回到原位，計時不中斷。需要 Chromium 系列瀏覽器的 Document Picture-in-Picture，不支援時不顯示這顆按鈕。

## 1.27.2

### Patch Changes

- bb06f3a: 王血量的版面調整：拿掉血量曲線，血條改成全寬、百分比移到血條下方，數字固定一位小數並鎖定字寬，血量跳動時版面不會跟著左右晃。

## 1.27.1

### Patch Changes

- 7038321: 修正多條血的王算成滿血的問題：扣掉的部分會露出「下一條血」的顏色而不是灰色空槽，右邊那段亮色是底不是血，血量只算從左端數過來的第一段。判斷是不是同一條血改看色相——同一條血從左到右有明暗漸層但色相不動，換一條血才是換色系。比例條也跟著改成遊戲裡的樣子：左邊是目前的血，右邊是露出來的下一條底色。

## 1.27.0

### Minor Changes

- 82154b1: 機制計算機新增「王血量」擷取：分享遊戲視窗後每秒判讀 BOSS 血條，顯示剩餘百分比、依實際血條顏色畫的比例條、最近的掉血速度與整場的血量曲線。判讀靠血條本身的外框（固定亮灰白）與內部雙色（高飽和＝有血、低飽和中灰＝空槽），不看色相也不寫死座標——血條顏色會變、多條血的王會同時有好幾種顏色、視窗大小一變位置與長度也跟著變。自動偵測失敗時可在畫面快照上框選範圍（存的是比例，換視窗大小不用重框）。這一區與王無關，任一機制模板的王都能開。

## 1.26.0

### Minor Changes

- 12f674c: 反盾面板改以「可輸出到」為主角：大字顯示可以打到幾分幾秒，本段剩餘秒數與 ±1 秒微調退為小字。時刻改由階段結束時間直接推出（新增 `attackEndAt()`），不再用「現在＋進位秒數」回推，時刻不會再每秒漂移。對齊遊戲計時後另外顯示持續走的當前遊戲計時，可隨時跟遊戲畫面核對。
- 12f674c: 「反盾計算機」更名為「機制計算機」，並把「反盾」抽成一份機制模板：王指定套用的模板（`mechanic`），模板決定規則、王只填自己的秒數。目前兩隻王都套反盾這一份，畫面與操作不變。
- 12f674c: 機制計算機新增「女皇」：五個機制各自固定間隔（活屍 60s、鎖潛能 90s、變豬 60s、反盾 80s、小黑屋 90s），只算多久觸發一次，不管持續與魔消。每個機制各自按「觸發」開始數、可 ±1 秒微調，到點自動接下一輪並響鈴，寬螢幕一列擺完五個。視覺沿用反盾面板同一套語言（色塊、引信邊框、進度條），並同樣有遊戲計時對齊與「接下來」時間表——對齊點兩個面板共用。
- 12f674c: 分寶紀錄列表每一列標出所屬的 DC 群組（沒有 groupId 的舊紀錄跟著第一個群組走）。

### Patch Changes

- 12f674c: 倒數改用 requestAnimationFrame 逐幀更新，並移除與之打架的 CSS transition：引信邊框與進度條不再一秒跳十格。

## 1.25.1

### Patch Changes

- 1fff072: 團員 handle 建議依群組分開：本機歷史只取同群紀錄裡出現過的人，不再把別團的團員混進建議清單（沒有 groupId 的舊紀錄跟著第一個群組走）。

## 1.25.0

### Minor Changes

- 166b69b: 新增「物品出售」截圖區塊：整區所有照片合併成串內一則訊息（內文固定「物品出售」），加圖／刪圖 PATCH 同一則、整區清空則刪掉該訊息；「掉落截圖」與「物品出售」加上 Discord 每則 10 張附件的上限提示與發佈前檢查。

## 1.24.0

### Minor Changes

- 084bad2: 反盾阻止成功時，倒數下方多一行小字顯示「可輸出還有 N 秒」——阻止成功後接的那段間隔也是能打的，算進來才知道實際還有多久。兩個倒數各自獨立，階段本身沒有合併。間隔中已標記魔消時同樣會把後面的阻止成功與下一段間隔算進去；下一輪魔消成不成功是未知數，不預測。跟本段倒數同值時不顯示（例如沒魔消的間隔）。

## 1.23.0

### Minor Changes

- 4e29a54: 團長辛苦費改為預設關閉——多數團只收 3% 手續費不收辛苦費。既有群組沒有這個欄位，所以升級後團長卡片會消失、有設辛苦費的紀錄金額也跟著變（已發佈的貼文需重新同步）；要繼續用辛苦費的群組請到設定頁把「啟用團長辛苦費」打開。

## 1.22.0

### Minor Changes

- 558a363: 反盾計算機加上 ±1 秒微調：大色塊倒數兩側各一顆按鈕，實況與倒數差一兩秒時就地校正，不必整段重按。倒數、進度條、引信與事件表都是從階段起點算出來的，所以微調等於整條時間軸一起平移，不會出現倒數改了但事件表沒跟上的情況。

  未同步提醒改成只管當下正在編輯的那一筆。原本關閉分頁時會掃全部紀錄，導致刻意留著不同步的場次（例如東西還在慢慢賣）每次都跳提醒；現在停在編輯頁才攔，離開編輯頁就不再提示。

## 1.21.0

### Minor Changes

- 6025803: 分配加入交易手續費：編輯頁「基本資料」多一個欄位，新紀錄預設 3%。手續費與團長辛苦費並列，都從團隊總額扣掉再均分，不是一層扣完再扣另一層。沒填的舊紀錄視為 0，既有金額完全不受影響。

  設定頁每個 DC 群組多一個「啟用團長辛苦費」開關。關掉之後編輯頁不出現「團長」卡片、團員列上的標記也不顯示、分配與 DC 輸出都不算辛苦費。未設定的既有群組視為啟用，行為不變。

  DC 輸出的總共行改寫成把百分比留在算式裡、註記用方括號，例如 `總共: 10000 * (1 - 3%[手續費] - 5%[辛苦費]) / 5 = 1840`。固定金額的辛苦費放在乘法外面減以保住運算順序。這個寫法讓匯入可以直接讀回百分比，辛苦費的百分比模式也不會再降級成固定金額。舊格式的貼文仍然讀得懂。

## 1.20.0

### Minor Changes

- 561a3a7: 反盾計算機用詞調整：「反盾失敗」一律改為「反盾阻止成功」。

  魔消擴充三項機制：王對魔消有 80 秒耐性、技能本身 60 秒冷卻（兩者都從施放當下起算），魔消持續改為預設 20 秒。耐性與冷卻只提醒不阻擋——按錯魔消成功要能改回來，所以耐性中照樣可以標記，只是畫面顯示剩餘秒數、不再閃爍催放、不響提示音、事件表也不列那一輪的魔消提示。冷卻用小字額外顯示。

  新增「反盾間隔浮動」參數（皮卡 3 秒、杜納斯 0 秒，可調）：反盾重施實際落在 間隔 ~ 間隔+浮動 之間。它只影響魔消時機（有效窗起點改用最晚重施時間往回推，buff 才撐得到），可輸出的判斷仍以間隔本身為準，該停手的時間點不變。

  上方大色塊加上引信效果：邊框繞著色塊隨倒數逐漸燒短。

## 1.19.1

### Patch Changes

- 78f820d: 團員清單裡的團長那一列改為唯讀：不能在該處改人或移除，這兩件事都在上方「團長」欄位做，兩處都能改會互相打架。

  修正全新使用者的名冊被自動帶入的問題：舊的來源設定找不到時預設值是「預設來源」，遷移時分不出「使用者選過預設來源」與「全新使用者什麼都沒有」，結果一開站就跟隨官方 repo 的名冊。改成直接檢查舊的 localStorage key 是否存在，全新使用者拿到的是空群組。已經被帶入的既有設定不會自動回退，要清請到設定頁把名單切成「自行輸入」。

## 1.19.0

### Minor Changes

- 567c6ea: appbar 右上角顯示目前版本號，點一下開「更新內容」彈窗。內容是執行期直讀 repo 上的 CHANGELOG.md（跟 members.json 同一個模式），所以改了 push 上去就看得到，不必為了更新文案發一次版；未來 CHANGELOG 裡放圖片也能正常顯示。用 snarkdown 渲染（gzip 約 1KB），並自動去掉只對開發者有意義的 commit hash 前綴。離線或抓取失敗時會提示並附上 GitHub 連結，版本號本身不依賴網路。

## 1.18.0

### Minor Changes

- 23cba24: 設定頁移回右上角的齒輪 ⚙（原本 Webhook 設定的位置），導覽列只留分寶紀錄／未領總覽／反盾計算機。另外加了一個隱藏捷徑：群組名稱取為「贖罪券」時，名單會自動接上本 repo 的 members.json 並立即抓取，不必自己貼網址。

## 1.17.0

### Minor Changes

- 2955f3f: 新增「設定」頁，把原本的名單管理與右上角 ⚙ 的 Webhook 設定合併進去，並擴充成多個 DC 群組。一個群組＝一套設定集（名稱、群組 Webhook、名單），每筆分寶紀錄綁定一個群組，發佈、同步、別名顯示與真 mention 都跟著紀錄自己的群組走——帶不同公會的團不會互相干擾，同一個人在兩個伺服器有不同暱稱也各顯示各的。舊的單一 webhook 與名冊會自動遷移成第一個群組，沒有 groupId 的舊紀錄一律歸屬於它。綁著紀錄的群組不能刪除，至少保留一個群組。

## 1.16.0

### Minor Changes

- 92529d2: 新增團長辛苦費：編輯頁可指定一位團長並設定辛苦費（團隊總額的百分比或固定金額）。辛苦費先從團隊總額扣掉，餘額均分給所有人，團長再額外拿走這筆。DC 匯出的總共行會寫成 `(10000 - 500(辛苦費)) / 5 = 1900`，匯入時可還原。同時修掉分配行 `500 = 500` 的冗餘寫法——沒有運算就不再重複寫一次金額。
- d63efeb: 名冊改為四個欄位：`discordHandle`、`discordNickName`、`discordId`、`alias`。Discord 那邊的名字與自己取的別名分開存，同步只覆蓋 discord\* 三欄，`alias` 永遠不會被蓋掉。顯示優先序為 `alias` → `discordNickName` → `discordHandle`。舊格式（`handle` / `alias` / `id`）會自動搬遷，localStorage 的舊快取在開站時轉換並立即寫回。新增 `pnpm members` 從 Discord 伺服器一次抓回所有成員（需 Bot Token，設定步驟見 docs/discord-bot-setup.md）。

### Patch Changes

- 92529d2: 修正代售漏算：代售是團員代替團隊賣掉的東西，收益屬於團隊，但先前沒有計入總額，導致那筆錢在分配時憑空消失（代售者被扣款，其他人卻沒分到）。改為「團隊總額＝總表淨額＋代售淨額」，分配面板的「總表淨額」隨之改稱「團隊總額」。有代售的既有紀錄金額會變成正確的值，已發佈到 DC 的貼文需要重新同步。

## 1.15.1

### Patch Changes

- 46b92dc: 修正手機版排版：導覽列等短標籤加上 `white-space: nowrap`，不再被擠成一字一行；窄螢幕（720px 以下）導覽列改為獨占一行可橫捲、編輯頁工具列與直播檔欄位自動換行、表格改為超出時橫向捲動而非壓縮所有儲存格。

## 1.15.0

### Minor Changes

- 29f77b1: 反盾計算機支援多王：標題列新增王的選單（皮卡啾／粉豆、杜納斯），每隻王各自記住反盾持續與間隔的調整；魔消持續改為玩家技能的全域參數，預設 15 秒。計時中鎖住換王，避免拿到錯的倒數。

## 1.14.0

### Minor Changes

- 8ec6258: 新增「反盾計算機」頁（皮卡啾/粉豆）：反盾/間隔/反盾失敗三態循環引擎，倒數自動推進＋反盾開始/反盾結束/反盾失敗開始三鈕隨時人工校準；魔消有效窗驗證（間隔最後 N 秒，太早按閃黃三下提示無效）；魔消有效中色塊綠 ⇄ 琥珀閃爍；聲音提醒（可輸出/魔消時機/禁止輸出，可關）；遊戲計時器 mm:ss 對齊（大色塊顯示下一階段時刻、事件表倒數時刻、±1s 校準）；反盾持續/間隔/魔消持續參數可調

## 1.13.0

### Minor Changes

- 1a2366a: DC 圖片全套：掉落/領錢/外購三類截圖（拖拉、Ctrl+V 貼上含區塊選單與 hover 提示、IndexedDB 暫存、一鍵同步增刪、CDN URL 過期自動刷新、刪除防護）；名單管理頁（名冊/品名清單三種來源：預設/自訂 URL/自行輸入，匯出 JSON）；列表顯示 DC 同步徽章；未同步離開/關閉分頁雙層提醒；autocomplete 名單載入提示

## 1.12.1

### Patch Changes

- b3c0ed7: 標題行與未領總覽場次行移除「/ 人數」（干擾閱讀；人數由團員清單推導、總共行仍有除數），解析相容新舊格式

## 1.12.0

### Minor Changes

- 2a3cf52: 編輯器開啟即檢查與 DC 貼文一致性（✓ 一致／● 不同步／⚠ 貼文已失效，編輯中即時反映、同步後復查）；非劃線項目的備註輸出為項目行下方縮排一行（先前未輸出會遺失），匯入相容還原

## 1.11.0

### Minor Changes

- bac917b: 發佈至 DC 支援真 mention：名冊 members.json 可填 Discord 使用者 ID，發佈時 @handle 自動轉 <@ID>（未填 ID 者維持原樣）；同步遇貼文/討論串已刪除（Discord 10003/10008）時詢問是否重新發文，其他 API 失敗不誤導重發；「綁定貼文」常駐可換綁

## 1.10.0

### Minor Changes

- 3de951f: 內購新增「全額／均攤」模式：均攤買家只付原價 1/N、其他人分實付額；輸出尾綴 (均攤)、匯入相容全/半形括號。品名遷移 v2：製作法家族統一全名（含百列 → 百烈）、珍珠手杖(+4AD) 併入龍之珍珠手杖且屬性移至註解；items.json 補寵魔 60%

## 1.9.0

### Minor Changes

- bcdd955: DC Webhook 發佈/同步（第一版）：⚙ 設定 dialog（貼 URL 即時驗證、清除）、發佈至 DC（論壇貼文，標題 [MM-DD]團名）、同步至 DC（PATCH 內文，按鈕四態回饋）、綁定貼文救援、429 自動重試；標題行改帶狀態標記（:shopping_cart:(待售項數) :dollar:(未結清人數)，全結案 :ballot_box_with_check:）；未填金額輸出 ? 取代 0（含剪刀價，parse 相容還原）

## 1.8.0

### Minor Changes

- 5dcea14: 紀錄可標記「擱置」（暫不列入未領總覽，列表顯示擱置徽章）；未領總覽每場新增「開啟 ↗」可開新分頁直達該紀錄的分配名單；紀錄狀態跨分頁即時同步

## 1.7.0

### Minor Changes

- a58bfa5: 新增「未領總覽」頁：未結清款項依團員分區塊、逐行複製、結清標記、待售標註；品名 autocomplete 改模糊比對（前綴/子字串/子序列計分）；品名標準化一次性遷移（潛能**%、手攻**%、大師附加、附加奇幻）

## 1.6.1

### Patch Changes

- d56a7db: 列表改依日期新到舊排序；同日期再依團名，空日期排最後。

## 1.6.0

### Minor Changes

- 53d0e21: - 列表新增兩個衍生狀態徽章：「全數賣出」（無待售項目）、「全部結清」（所有團員已結清）。
  - 修正 autocomplete 下拉被表格 overflow 容器裁切的問題：改用 fixed 定位並隨捲動重算，
    浮於最上層可輕易選取。
  - 歷史金額改為純參考（tooltip），選取品名時不再自動填入單價。

## 1.5.0

### Minor Changes

- cb83a41: - 共用品名清單：app 執行期直讀 repo 的 items.json（raw fetch + localStorage 快取），
  併入品名 autocomplete；更新只需 push git、不必發版。
  - 「王名」欄位標籤改為「團名」。
  - 標記「不計入」時自動帶上註解「(價格過低不計入)」（已有自訂註解則保留；離開不計入
    且為自動值時清除）。

## 1.4.0

### Minor Changes

- 6dbe7fc: 共用團員名冊與別名：app 執行期直讀 repo 的 members.json（raw fetch + localStorage
  快取），更新名冊只需 push git、不必發版。團員 handle 對應別名後於 UI 顯示（團員列、
  內購、代售、分配），DC 序列化/解析仍用 handle 以便貼回自動識別。建議選單 =
  共用名冊 ∪ 本機歷史，下拉顯示「別名 (handle)」。

### Patch Changes

- f54f31e: 總表新增項目調整：狀態預設「待售」、數量預設空白（單價/剪刀價/剪刀數本就空白）。
  修正品名/王名 autocomplete：還原為前綴比對（別名欄才用顯示文字子字串比對）。
- 62aef47: 狀態循環順序改為 待售 → 售出 → 不計入；不計入圖標改用 ✖（DC 輸出 struck 行改
  `:heavy_multiplication_x:`，解析相容舊的 `:shopping_cart:`）。

## 1.3.0

### Minor Changes

- d93561a: 代售支援剪刀成本：代售的物品若需消耗剪刀交易，剪刀成本從代售者持有額扣除
  （持有淨額 = 單價 × 數量 − 剪刀單價 × 剪刀數），同總表。編輯卡片加剪刀價/剪刀數欄，
  DC 代售行支援 `= 300x1 - 80(剪刀)x2` 寫法，匯入相容。

## 1.2.0

### Minor Changes

- db925df: 新增「代售」功能：記錄某團員代賣的物品／金額（獨立清單，代售者限團員），
  併入分配結算——結算 = 收入 − 自己代售額，避免金幣二次轉手被遊戲扣手續費。
  分配面板於有代售時顯示「代售」「結算」兩欄；DC 格式新增 `## 代售` 區塊、
  分配行末端補 `- <代售額>`，匯入相容。

## 1.1.0

### Minor Changes

- bf1742e: 分配金額改為無條件進位（`Math.ceil`）：每人基本、他人內購/(人數 −1)、個人收入與序列化輸出一律向上取整，避免因四捨五入使團員少拿。

## 1.0.1

### Patch Changes

- c8f5b1d: 修正發版不產生 git tag 的問題：private 套件在 changeset 預設下不被打 tag，
  於 config.json 設 `privatePackages: { version: true, tag: true }`；並將
  `changeset tag` 移到 `# Version` commit 之後，使版本 tag 指向 package.json
  已是新版號的 commit。

## 1.0.0

### Major Changes

- 46ea76d: 首次正式發佈：DC 打王分寶 editor

  - 表單化編輯：總表（單價 × 數量、剪刀成本）、內購（限團員）、直播檔、即時分配面板
  - 分配公式：總表均分 + 他人內購/(人數 −1) − 自己內購；人數由團員數推導
  - 貼上／複製 DC round-trip，相容 emoji 與短碼、@名稱與 <@數字 ID>
  - 歷史建議（品名、團員、帶日期單價、王名）、多筆紀錄以 localStorage 保存
  - pnpm + changeset 版本管控、GitHub Actions 手動發版部署至 GitHub Pages
