---
'boss-toolkit': minor
---

專案改名為「打王工具箱」。原本的名字只講到分寶，但實際上用得最多的是機制計算機，名字對不上內容。GitHub repo 一併改成 `boss-toolkit`，網址跟著變成 <https://unrealsky.github.io/boss-toolkit/>——舊網址的書籤要重設。存在瀏覽器裡的紀錄不受影響（localStorage 綁的是網域，不是路徑）。
