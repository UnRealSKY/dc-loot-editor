---
'dc-loot-editor': patch
---

修正三件子母畫面的問題：一是選到女皇時 `now` 停止更新，導致遊戲計時對齊後顯示的時間會愈按愈偏（輸入 44:00 卻顯示 44:23）；二是把遊戲計時放進原本「重置」那一列（左對齊、右重置），不再多出一顆重置按鈕；三是女皇的「接下來」時間表拆成獨立元件留在主視窗——子母畫面是把面板搬過去而不是複製，原本整個面板連同時間表一起被搬走，主視窗就空了。
