---
"dc-loot-editor": minor
---

DC 圖片全套：掉落/領錢/外購三類截圖（拖拉、Ctrl+V 貼上含區塊選單與 hover 提示、IndexedDB 暫存、一鍵同步增刪、CDN URL 過期自動刷新、刪除防護）；名單管理頁（名冊/品名清單三種來源：預設/自訂 URL/自行輸入，匯出 JSON）；列表顯示 DC 同步徽章；未同步離開/關閉分頁雙層提醒；autocomplete 名單載入提示
